from django.urls import path
from . import views

app_name = 'authentication'

urlpatterns = [
    # Authentication URLs will be added here
    # Example: path('register/', views.RegisterView.as_view(), name='register'),
]

# apps/authentication/urls.py