from django.urls import path
from . import views

app_name = 'analytics'

urlpatterns = [
    # Analytics URLs will be added here
    # Example: path('summary/', views.SummaryView.as_view(), name='summary'),
]


# apps/analytics/urls.py