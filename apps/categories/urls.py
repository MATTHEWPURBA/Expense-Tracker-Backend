from django.urls import path
from . import views

app_name = 'categories'

urlpatterns = [
    # Category URLs will be added here
    # Example: path('', views.CategoryListView.as_view(), name='list'),
]

# apps/categories/urls.py