from django.urls import path
from . import views

app_name = 'transactions'

urlpatterns = [
    # Transaction URLs will be added here
    # Example: path('', views.TransactionListView.as_view(), name='list'),
]

# apps/transactions/urls.py